"use strict";
describe("Data Type", function () {
    it('should must declare', function () {
        let name = "Aliffio Syah Maulana";
        let balance = 1000000;
        let isVip = true;
        console.info(name);
        console.info(balance);
        console.info(isVip);
        // name = 100;
        // balance = "1000000";
        // isVip = 100;
    });
});
